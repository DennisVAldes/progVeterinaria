Public Class classPersona
    Dim name As String
    Dim ci As Integer
    Dim dir As String
    Dim tel As Integer

    Public Sub altaPersona(ci, name, dir, tel)
        ci = Me.ci
        name = Me.name
        dir = Me.dir
        tel = Me.tel
    End Sub

    Public Sub altaPersona()

    End Sub
End Class
